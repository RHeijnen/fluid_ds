node_modules
dist
build
storybook-static
# Unified deploy artifact assembled by `pnpm build:website`. ~18 MB.
# Deployment uploads from here; nothing in here is hand-edited.
website
# Astro stores its generated content type definitions and route data
# under .astro/. Regenerated on every `astro dev`/`astro check`, no
# value in tracking it in git.
.astro
.cache
.turbo
*.log
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
coverage
apps/benchmarks/results.json
apps/benchmarks/expansion-results.json
apps/benchmarks/*.heapsnapshot
# Runtime evidence is normally machine-local. The bounded dependency patch proof
# is release input, however, and must remain available in a clean checkout.
quality/evidence/*
!quality/evidence/dependency-risk/
quality/evidence/dependency-risk/*
!quality/evidence/dependency-risk/local-patches/
quality/evidence/dependency-risk/local-patches/*
!quality/evidence/dependency-risk/local-patches/extract-zip-2.0.1-linux.json
.codex-tmp/
**/playwright-report/
**/test-results/
**/.svelte-kit/
*.tsbuildinfo
.env
.env.local
# Per-machine runtime lock file the Claude Code harness writes when a
# scheduled task is active. Nothing useful to share across machines.
.claude/scheduled_tasks.lock
