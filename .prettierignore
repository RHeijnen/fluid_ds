node_modules
dist
build
storybook-static
.cache
.turbo
coverage
pnpm-lock.yaml
**/custom-elements.json
packages/tokens/dist
# Angular CLI build cache (generated, not source).
.angular
# Next.js build output (generated by the unified website build).
.next
apps/admin-next/out
# Generated fixtures/catalogs. Their exact shape is parsed by quality gates
# (scripts/check-quality.mjs reads `"tag": "..."`), so reformatting breaks them.
**/.generated
# Retained certification evidence: logs, run manifests and provenance records.
# These are records of what was executed, not source. Reformatting them edits
# the evidence itself (eslint already ignores quality/evidence for the same
# reason), so they are excluded here too.
**/evidence
**/candidate-evidence
# Generated by scripts/check-quality.mjs (JSON.stringify), not hand-edited.
quality/component-quality.json
# React wrappers are generated from the canonical CEMs and byte-verified
# against the generator by packages/react/scripts/check-generated.mjs, so
# their formatting is the generator's domain, not prettier's.
packages/react/src/jsx
packages/react/src/jsx.ts
packages/react/src/generated
# The pinned framework corpus is written by
# scripts/prepare-framework-pinned-corpus.mjs and each file's bytes are
# sha256-bound in framework-pinned-profile.json; reformatting breaks the
# binding.
scripts/fixtures/framework-pinned
