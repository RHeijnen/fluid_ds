{
  "name": "@fluid-ds/admin-native",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Admin-portal demo built with plain HTML + the Fluid web components loaded from the CDN (no framework, no bundler, no dependencies).",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build-static.mjs"
  }
}
